import {} from "@testing-library/react";
import { describe, it } from "node:test";

describe("Testa o arquivo de Login", () => {
  it("Testa se há valor na constante", () => {
    const testNumber = 10;
    expect(testNumber).toBe(10);
  });
});
